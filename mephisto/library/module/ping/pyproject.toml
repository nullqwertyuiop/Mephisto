[project]
name = "library.module.ping"
version = "0.1.0"
description = "检查服务是否在线"
dependencies = []
authors = [
    { name = "nullqwertyuiop", email = "null@member.fsf.org" }
]

[tool.pdm]
distribution = false

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[mephisto]
name = "Ping"
identifier = "library.module.ping"
dependencies = []
standards = []
